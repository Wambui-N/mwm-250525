import React from 'react';

const FinalCTASection = () => (
  <section className="text-center space-y-4">
    <h2 className="text-2xl font-bold">Want to stop writing proposals from scratch?</h2>
    <p className="text-lg">Let's build one that writes itself.</p>
    <a href="#book-call" className="inline-block bg-black text-white px-6 py-3 rounded-lg font-semibold hover:bg-gray-800 transition">👉 Book a Discovery Call</a>
    <div className="text-gray-500">→ Or try the interactive demo below.</div>
  </section>
);

export default FinalCTASection; 